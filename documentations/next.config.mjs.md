# next.config.mjs

## Purpose
Holds the Next.js configuration object.

## Exports
- `nextConfig`

## Notes
- Currently empty (`/* config options here */`), but it is the place to add future build/runtime configuration as needed.
